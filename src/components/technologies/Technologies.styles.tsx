import { Grid } from '@material-ui/core'
import styled from 'styled-components'

export const UL = styled(Grid)`
  text-align: center;
  display: flex;
  justify-content: center;
  margin-top: 20px;
  & div {
    font-size: 100px;
  }
`
